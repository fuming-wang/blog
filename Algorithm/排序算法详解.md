```c
void insertSort(int* nums, int numsSize) {
	for (int i = 1; i < numsSize; i++) {
		if (nums[i] < nums[i - 1]) {
			int tmp = nums[i];
			int j = i - 1;
			for (; j >= 0 && nums[j] > tmp; j--) {
				nums[j + 1] = nums[j];
			}
			nums[j + 1] = tmp;
		}
	}
}
```